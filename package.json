{
  "name": "definer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ serman88/definer",
    "create-local": "graph create --node http://localhost:8020/ serman88/definer",
    "remove-local": "graph remove --node http://localhost:8020/ serman88/definer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 serman88/definer"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
